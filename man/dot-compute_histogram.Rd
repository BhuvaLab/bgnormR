% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmm_utils.R
\name{.compute_histogram}
\alias{.compute_histogram}
\title{Precompute a histogram of log-transformed intensities for storage}
\usage{
.compute_histogram(x_log, bins = 100L)
}
\arguments{
\item{x_log}{Numeric vector of log-transformed intensities (may contain
zeros and non-finite values; these are excluded before binning).}

\item{bins}{Integer number of histogram bins (default 100).}
}
\value{
A list with elements \code{breaks} (length bins + 1) and
  \code{density} (length bins), as returned by \code{hist(..., plot = FALSE)}.
}
\description{
Used during \code{bgnorm_pixels} and \code{bgnorm_cells} fitting to store a
compact histogram of the raw data so that \code{plot_distributions} does not
need the original image or assay.
}
\keyword{internal}
