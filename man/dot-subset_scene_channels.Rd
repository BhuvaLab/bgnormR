% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qptiff_metadata.R
\name{.subset_scene_channels}
\alias{.subset_scene_channels}
\title{Align a scene's channel metadata to the loaded channels}
\usage{
.subset_scene_channels(meta, all_names, ch_idx)
}
\arguments{
\item{meta}{A \code{QPTIFFMetadata} object (single scene).}

\item{all_names}{Character vector of all channel names (length = n channels).}

\item{ch_idx}{Integer indices of the loaded channels.}
}
\value{
\code{meta} with its primary scene's channels aligned and subset.
}
\description{
Normalises the primary scene's channel list to \code{length(all_names)}
entries (padding with name-only stubs so the list is never shorter than the
image), then subsets it to the loaded channels \code{ch_idx}.  Keeps the
stored per-channel metadata 1:1 with \code{names(img)}.
}
\keyword{internal}
