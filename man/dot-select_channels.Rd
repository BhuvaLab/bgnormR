% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qptiff_metadata.R
\name{.select_channels}
\alias{.select_channels}
\title{Resolve a channel selection to indices + names}
\usage{
.select_channels(channels, all_names)
}
\arguments{
\item{channels}{User channel selection (character / numeric / NULL).}

\item{all_names}{Character vector of all available channel names.}
}
\value{
List with \code{ch_idx} (integer) and \code{ch_names} (character).
}
\description{
Shared by all readers: turns a \code{channels} argument (names, 1-based
integer indices, or \code{NULL} = all) into aligned \code{ch_idx} /
\code{ch_names}, erroring with the available set when a name is not found.
}
\keyword{internal}
