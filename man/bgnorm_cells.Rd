% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bgnorm_cells.R
\name{bgnorm_cells}
\alias{bgnorm_cells}
\title{Cell-level background normalisation (bgnorm)}
\usage{
bgnorm_cells(
  x,
  cofactor = 150,
  quantile_norm = FALSE,
  quantile = 0.75,
  sample_prop = 1,
  ...
)
}
\arguments{
\item{x}{Numeric vector of raw cell-level intensities for one marker.
Must be a plain vector; matrices and arrays are not accepted.}

\item{cofactor}{Positive numeric cofactor for log2 transform (default 150).}

\item{quantile_norm}{Logical; apply model-based quantile normalisation?}

\item{quantile}{Quantile of the signal component for normalisation
(default 0.75).}

\item{sample_prop}{Numeric in (0, 1]; proportion of non-zero observations
used to fit the GMM.  Default \code{1} (use all).}

\item{...}{Additional arguments forwarded to the internal GMM fitter.}
}
\value{
A \code{BgnormResult} object.
}
\description{
Applies a two-component Gaussian Mixture Model to cell-level aggregated
intensities for a single marker.  The two components represent
non-specific binding / autofluorescence and biological signal.
}
\details{
This is a cell-level approximation of the pixel-level method intended for
cases where only cell-summarised intensities (e.g., mean or median
intensity per cell) are available.
}
\examples{
set.seed(3)
# Simulate cell intensities: nonspecific + signal
x <- c(exp(rnorm(300, log(200), 0.5)), exp(rnorm(100, log(1500), 0.8)))
res <- bgnorm_cells(x)
print(res)
}
\seealso{
\code{\link{bgnorm_pixels}}, \code{\link{bgnorm_sce}}
}
