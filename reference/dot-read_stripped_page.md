# Read a stripped TIFF page (handles uncompressed + DEFLATE)

Read a stripped TIFF page (handles uncompressed + DEFLATE)

## Usage

``` r
.read_stripped_page(path, layout, h, w, bps, compression, rows, cols)
```
