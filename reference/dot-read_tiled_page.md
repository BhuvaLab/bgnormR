# Read a tiled TIFF page (handles uncompressed + DEFLATE)

Read a tiled TIFF page (handles uncompressed + DEFLATE)

## Usage

``` r
.read_tiled_page(path, layout, h, w, bps, compression, rows, cols)
```
